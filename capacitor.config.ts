import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.commutecart.user.app',
  appName: 'Book My Driver',
  webDir: 'dist/commute-cart-user',
  bundledWebRuntime: false
};

export default config;
